# 절대 커밋 금지 - 민감 정보
terraform.tfvars
*.tfvars
!*.tfvars.example

# Terraform 상태 (로컬 백엔드 사용 시 커밋 금지 - 시크릿 포함 가능)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# 크래시 로그
crash.log
crash.*.log

# 오버라이드 파일
override.tf
override.tf.json
*_override.tf
*_override.tf.json
